# @nimbalodge/types

Placeholder créé en Phase 1 pour réserver l'emplacement dans l'arborescence cible (brief §42).

Sera peuplé à partir de la Phase 2 avec les types métier partagés entre `apps/web`, `apps/api` et
`apps/mobile` (Organization, Hotel, Department, Activity, CostCenter, Revenue, Expense, Budget,
Reservation, Room, Guest, Employee, etc.), en miroir du schéma Prisma une fois le backend en place.
