node_modules/
dist/
dist-ssr/
.turbo/
*.tsbuildinfo
*.local
.DS_Store
*.log
.env
.env.*
!.env.example
!.env.test.example

# Étape 7 — fichiers uploadés (LocalDiskStorageProvider, dev uniquement) : jamais commités.
# Chemin relatif à process.cwd() du process API (la racine du repo dans tous les scripts npm de ce
# projet — dev/build/e2e lancent tous node/jest depuis la racine, pas depuis apps/api/).
/uploads-data/
